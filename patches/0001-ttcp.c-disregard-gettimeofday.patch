diff --git a/ttcp.c b/ttcp.c
index 89e5374..f677cfb 100644
--- a/ttcp.c
+++ b/ttcp.c
@@ -533,6 +533,7 @@ getrusage(ignored, ru)
     ru->ru_utime.tv_usec = ((buf.tms_utime % HZ) * 1000000) / HZ;
 }
 
+#if ! defined(__CYGWIN__)
 /*ARGSUSED*/
 static 
 gettimeofday(tp, zp)
@@ -542,6 +543,7 @@ gettimeofday(tp, zp)
     tp->tv_sec = time(0);
     tp->tv_usec = 0;
 }
+#endif
 #endif /* SYSV */
 
 /*
